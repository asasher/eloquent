interface:
  display_name: "Resume Skills"
  short_description: "Resume, job search, and career docs"
  default_prompt: "Use $resume-skills to optimize my resume for a target job."

policy:
  allow_implicit_invocation: true
